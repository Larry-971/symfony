{% extends "base.html.twig" %}

{% block body %}
    <h2>Formulaire d'ajout</h2>

    <!-- Donner du style à mon formulaire -->
    {{ form_start(form_produit) }}

        <div class="form-group">
            <div class="col-md-8">
                {{ form_label(form_produit.nom)}}
                {{ form_widget(form_produit.nom, {"attr" : {"class" : "form-control", "placeholder" : "Entrez le nom"} }) }}
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-8">
                {{ form_label(form_produit.marque) }}
                {{ form_widget(form_produit.marque, {"attr" : {"class" : "form-control", "placeholder" : "Entrez la marque"} }) }}
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-8">
                {{ form_label(form_produit.prix) }}
                {{ form_widget(form_produit.prix, {"attr" : {"class" : "form-control", "placeholder" : "Entrez le prix"} }) }}
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-8">
                {{ form_label(form_produit.image) }}
                {{ form_widget(form_produit.image, {"attr" : {"class" : "form-control", "placeholder" : "Entrez votre image"} }) }}
            </div>  
        </div>

        <div class="form-group">
            <div class="col-md-8">
                {{ form_label(form_produit.description) }}
                {{ form_widget(form_produit.description, {"attr" : {"class" : "form-control", "placeholder" : "Entrez le nom"} }) }}
            </div>
        </div>

    {{ form_end(form_produit) }}

    </div>  
{% endblock %}